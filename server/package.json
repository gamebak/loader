{
  "name": "webtools",
  "version": "1.0",
  "scripts": {
    "server": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1"
  },
  "engines": {
    "node": ">=16",
    "yarn": "use npm instead of yarn"
  }
}
